client:
  name: "Cliente Ejemplo"
  id: "cliente_ejemplo"

scraper:
  source: "firecrawl"          # firecrawl (recomendado) | indeed
  location: "España"
  queries:
    - "data entry"
    - "administrativo excel"
    - "grabador datos"
    - "auxiliar administrativo excel"
    - "facturación administrativa"
  max_pages_per_query: 3
  max_results: 50

enricher:
  model: "claude-haiku-4-5-20251001"
  batch_size: 5
  min_score: "low"          # exportar: high | medium | low | all

delivery:
  google_sheets:
    enabled: true
    spreadsheet_id: ""      # vacío = usa GOOGLE_SHEETS_SPREADSHEET_ID del .env
    worksheet_name: "Leads"
